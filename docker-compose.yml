version: '3.8'

services:
  ai-hub-webpage:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-hub-webpage
    restart: always
    ports:
      - "4321:80"
      - "8080:80"
