# Що змінено

<!-- Одне-два речення. Що саме і навіщо. -->

Пов'язана задача:

---

## Чекліст автора

### Обсяг змін

- [ ] Диф містить **тільки** те, що потрібно для цієї задачі
- [ ] Я подивився `git diff --stat` і можу пояснити кожен файл у списку одним реченням
- [ ] Немає «заодно виправив» правок

### Заморожені файли (AGENTS.md §1)

Позначте, якщо змінювали, і поясніть чому:

- [ ] `Header.astro` — причина: ␣
- [ ] `Footer.astro` — причина: ␣
- [ ] `Layout.astro` — причина: ␣
- [ ] `tailwind.config.mjs` / `tokens.ts` — причина: ␣
- [ ] `nginx.conf` / `Dockerfile` — причина: ␣
- [ ] `.snapshots/layout.json` оновлено свідомо — що саме змінилось: ␣

*Якщо жодного не позначено — добре, це очікуваний стан.*

### Дизайн

- [ ] Жодного hex-кольору в `.astro` — тільки токени
- [ ] Жодного нового інлайнового `<svg>` — іконки з `src/design/icons.ts`
- [ ] Жодного емодзі у ролі іконки
- [ ] Усі класи Tailwind існують у **v3.4** (не v4)
- [ ] Перевірено на 320 / 768 / 1440 / 1920 px

### Контент

- [ ] Дані читаються з `src/data/`, нічого не продубльовано в розмітку
- [ ] Тексти не змінювались, якщо про це не просили
- [ ] Бренд написано як `AI Hub` (велика латинська **I**)

### Аналітика та приватність

- [ ] У події аналітики **не** потрапляють ПД (email, телефон, ПІБ, вміст форм)
- [ ] Нові трекери не додавались, або додані з прив'язкою до згоди

### Перевірки (вивід у коментарі до PR)

- [ ] `npx astro check` — **0 errors**
- [ ] `npm run build` — без warnings
- [ ] `node scripts/check-design-system.mjs` — OK
- [ ] `node scripts/check-layout-snapshots.mjs` — OK

---

## Помічено, але НЕ змінено

<!-- Проблеми, які побачили поруч, але свідомо не чіпали. Файл:рядок + опис.
     Порожньо теж буває — але зазвичай щось є. -->

-

---

## Потребує рішення людини

<!-- Питання, які заблокували роботу або де були варіанти. -->

-

---

## Деплой

- [ ] Готово до деплою на **DEV**
- [ ] На **PROD** — тільки після окремої явної команди Myk
